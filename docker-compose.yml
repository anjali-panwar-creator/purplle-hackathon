version: '3.8'

services:
  intelligence-api:
    build: 
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - intelligence-api
    restart: always
